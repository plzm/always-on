name: Test env var string ops

on:
  workflow_dispatch:
  push:
    branches:
      - test

env:
  INFIX: '15'
  NAME_1: 'MyName1-$INFIX'
  NAME_2: 'MyName2-$INFIX'

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Echo env vars
        run: |
          echo $INFIX
          echo ${{ env.NAME_1 }}
          echo ${{ env.NAME_2 }}
